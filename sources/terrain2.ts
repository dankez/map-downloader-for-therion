import type { LayerConfig } from '../types';

export const terrain2: LayerConfig = {
  id: 'terrain2',
  nameKey: 'layerTerrain2',
  type: 'xyz',
  urlPattern: 'https://dmr5-shading.tiles.freemap.sk/{z}/{x}/{y}.jpg',
};
